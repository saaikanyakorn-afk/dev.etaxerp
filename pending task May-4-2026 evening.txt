========================================================
  PENDING TASKS — May 4, 2026 (Evening Session)
  Agent: Kai | Project: E-Tax Center (etaxerp.com)
========================================================

──────────────────────────────────────────────────────
1. WHAT พี่ทราย ASKED TO DO / FIX
──────────────────────────────────────────────────────

(A) Implement Billing-Note Public Share Link
    - Add a public share page for billing notes (no login required)
    - Link sharing via copy-link and LINE button inside billing-notes list
    - Share URL format: /share/billing-note/:token
    - OG image preview when link is shared on LINE/Facebook

(B) Fix Petty Cash Recording Error
    - There was an error when recording petty cash transactions
    - Fix both frontend form and backend route error handling

(C) Implement WHT Certificate Share
    - Add a public share page for withholding tax (ภ.ง.ด.) certificates
    - Share URL format: /share/wht-cert/:token
    - Similar pattern to existing invoice/receipt share pages

(D) Build ระบบตรวจสอบ — Invoice Recompute Preview
    - Path: /dev/recompute-preview
    - Standalone HTML page (no React) inside dev-menu-routes.ts
    - Preview which invoices have incorrect payment status
    - Allow admin to recompute and apply fixes
    - This was already completed in a prior session

(E) Push all completed items to production
    - After all 4 items above are done, push files to GitHub repo
      (saaikanyakorn-afk/etaxcenter, branch: main)


──────────────────────────────────────────────────────
2. WHAT WAS DONE TO FIX THEM
──────────────────────────────────────────────────────

(A) Billing-Note Share — COMPLETED IN DEV
    - Created: client/src/pages/finance/billing-note-share.tsx
      (new public page, no auth required, fetches doc by share token)
    - Updated: client/src/pages/finance/billing-notes.tsx
      (copy-link and LINE share button now generate & use share token)
    - Updated: server/routes/billing-notes-routes.ts
      (added POST /api/billing-notes/:id/share to create token,
       added GET /api/public/billing-note/:token for public info,
       added GET /api/public/billing-note/:token/pdf for public PDF)
    - Updated: client/src/app-extra.tsx
      (added /share/billing-note/:token route → BillingNoteShare page)
    - Updated: client/src/pages/not-found.tsx
      (injects AppExtra so unknown /share/* paths are caught)
    - Updated: server/static.ts
      (added route: GET /share/billing-note/:token → shareOgHandler)
    - Updated: server/share-og.ts
      (added billing-note to OG image color/icon map)

(B) Petty Cash Fix — COMPLETED IN DEV
    - Updated: client/src/pages/petty-cash.tsx
      (fixed frontend form submission error)
    - Updated: server/routes/petty-cash-routes.ts
      (improved error handling and access control for petty cash routes)

(C) WHT Certificate Share — COMPLETED IN DEV
    - Created: client/src/pages/purchases/wht-cert-share.tsx
      (new public share page for ภ.ง.ด. certificates)
    - Updated: client/src/app-extra.tsx
      (added /share/wht-cert/:token route → WhtCertShare page)
    NOTE: server/static.ts already had the wht-cert route registered
    from a prior session.

(D) Invoice Recompute Preview — COMPLETED IN DEV
    - Updated: server/routes/dev-menu-routes.ts
      (standalone HTML page at GET /dev/recompute-preview,
       API at GET /api/dev/invoice-recompute-preview,
       API at POST /api/dev/invoice-recompute-apply)

(E) Cleanup — COMPLETED
    - Commented out runFirmClientMigration in shared/schema-extra.ts
      (column target_db_machine_id already exists on production — no-op)
    - Removed runFirmClientMigration call from server/routes/firm-routes.ts


──────────────────────────────────────────────────────
3. STEP THAT COULD NOT BE CONTINUED — AND WHY
──────────────────────────────────────────────────────

BLOCKED: Cannot push files to production GitHub repository.

Reason:
  The Replit environment has a secret named GITHUB_PAT_PRODUCTION
  which is the GitHub Personal Access Token used to push files
  to the production repo (saaikanyakorn-afk/etaxcenter).

  When the push was attempted, GitHub API returned:
    "Bad credentials"

  This means the token stored in GITHUB_PAT_PRODUCTION is either:
    - Expired (GitHub PATs can expire)
    - Revoked
    - Incorrect value stored in the Replit Secret

  Resolution needed:
    - พี่ช้าง (or whoever manages the GitHub token) must generate
      a new GitHub Personal Access Token with repo write permission
      and update the GITHUB_PAT_PRODUCTION secret in this Replit project.
    - Once updated, the push can be completed in the next session
      by running the same push script.

Additional note:
  Per project rules, production pushes also require พี่ช้าง authorization
  before executing. This session had authorization ("push what needed")
  from the user, but could not proceed due to the token issue.


──────────────────────────────────────────────────────
4. FULL LIST OF FILES WAITING TO PUSH TO PRODUCTION
──────────────────────────────────────────────────────

All files below are complete and tested in dev.
None have been pushed to production yet.

  # WHT Certificate Share
  client/src/pages/purchases/wht-cert-share.tsx       (NEW FILE)
  client/src/app-extra.tsx                             (updated — WHT + billing-note routes)

  # Petty Cash Fix
  client/src/pages/petty-cash.tsx                     (updated)
  server/routes/petty-cash-routes.ts                  (updated)

  # Invoice Recompute Preview (ระบบตรวจสอบ)
  server/routes/dev-menu-routes.ts                    (updated)

  # Billing-Note Public Share
  client/src/pages/finance/billing-note-share.tsx     (NEW FILE)
  client/src/pages/finance/billing-notes.tsx          (updated)
  server/routes/billing-notes-routes.ts               (updated)
  client/src/pages/not-found.tsx                      (updated)
  server/static.ts                                    (updated)
  server/share-og.ts                                  (updated)

  # Migration Cleanup (safe to push, no DB changes)
  shared/schema-extra.ts                              (migration commented out)
  server/routes/firm-routes.ts                        (migration call removed)

  TOTAL: 13 files (2 new, 11 updated)

──────────────────────────────────────────────────────
  PROTECTED FILES (never push these):
  App.tsx, app.ts, server/index.ts, routes.ts,
  storage.ts, db.ts, shared/schema.ts
──────────────────────────────────────────────────────

END OF DOCUMENT
