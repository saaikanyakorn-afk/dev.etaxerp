========================================================
PENDING PRODUCTION DATABASE CHANGES
Maintained by: Kai (agent)
Rule: Update THIS FILE before ANY database change on dev.
      พี่ช้าง must approve before running anything on production.
========================================================

HOW TO USE THIS FILE
--------------------
- Every planned DB change is logged here FIRST (schema + data).
- Format each entry: [Date] [Status] [What] [Why] [Risk]
- Status values: PENDING | APPROVED | DONE | ROLLED BACK

========================================================
ENTRY #001 — expenses table: 3 new columns
Date: 2026-05-05/06
Status: PENDING (dev DB has columns, production does NOT)
Approved by: (waiting)
========================================================

WHAT:
  Three new columns added to the `expenses` table.

WHY:
  Feature: foreign currency support + outstanding balance display
  for expense module.
  - currency_code: store original foreign currency (USD, EUR, etc.)
  - exchange_rate: THB per 1 unit of foreign currency at time of entry
  - paid_amount: track how much of AP balance has been settled

SQL TO RUN ON PRODUCTION (in order):

  ALTER TABLE expenses
    ADD COLUMN IF NOT EXISTS currency_code TEXT NOT NULL DEFAULT 'THB';

  ALTER TABLE expenses
    ADD COLUMN IF NOT EXISTS exchange_rate DECIMAL(15,6) NOT NULL DEFAULT 1;

  ALTER TABLE expenses
    ADD COLUMN IF NOT EXISTS paid_amount DECIMAL(15,2) NOT NULL DEFAULT 0;

RISK: Very low.
  - IF NOT EXISTS: safe to re-run, no error if column already exists.
  - DEFAULT values: no existing rows affected.
  - Additive only: no existing columns modified or dropped.

VERIFY AFTER RUNNING:
  SELECT column_name, data_type, column_default
  FROM information_schema.columns
  WHERE table_name = 'expenses'
    AND column_name IN ('currency_code', 'exchange_rate', 'paid_amount')
  ORDER BY column_name;
  -- Expected: 3 rows

ROLLBACK (only with authorisation + simultaneous code revert):
  ALTER TABLE expenses DROP COLUMN IF EXISTS currency_code;
  ALTER TABLE expenses DROP COLUMN IF EXISTS exchange_rate;
  ALTER TABLE expenses DROP COLUMN IF EXISTS paid_amount;

RELATED CODE FILES:
  server/routes/expense-routes.ts — reads/writes these columns via raw SQL
  client/src/pages/purchases/expense.tsx — sends currencyCode/exchangeRate
  client/src/pages/purchases/expense-list.tsx — displays outstanding/total

NOTE: ALTER TABLE code was REMOVED from expense-routes.ts (commit cd21f4f9)
      after unauthorised auto-run was discovered. The columns now exist
      only because they were run once on dev during development.
      Production requires manual authorised run of the SQL above.

========================================================
ENTRY #002 — general_settings.bot_api_key — CANCELLED (DO NOT RUN)
Date: 2026-05-07
Status: CANCELLED — per-company key UI was removed; BOT_API_KEY stays platform env var only
Approved by: N/A
========================================================

WHAT:
  Was: add bot_api_key TEXT to general_settings.
  Column exists on dev DB only (harmless, no data in it).

WHY CANCELLED:
  พี่ทราย confirmed: exchange rate config is platform-level (developer sets BOT_API_KEY
  as Replit env var). End-users must NOT configure this. Per-company UI removed.
  The settings tab "อัตราแลกเปลี่ยน" removed from settings-tabs.tsx.

DO NOT RUN ON PRODUCTION — column not needed.
If dev column causes issues, run: ALTER TABLE general_settings DROP COLUMN IF EXISTS bot_api_key;

========================================================
ENTRY #003 — billing-notes-routes.ts: fix invoice totalAmount formula (CODE ONLY, no DB schema change)
Date: 2026-05-07
Status: APPROVED — authorized by พี่ช้าง (push to production immediately)
Approved by: พี่ช้าง
========================================================

WHAT:
  Code-only fix in server/routes/billing-notes-routes.ts — 2 creation paths.
  No DB schema change. No migration needed.

WHY:
  Two invoice creation paths were calculating WHT but NOT subtracting it from
  totalAmount, causing wrong records (total = subtotal + VAT instead of
  subtotal + VAT - WHT).

  Correct formula: totalAmount = subtotal + VAT - WHT
  Wrong formula (old):
    Path 1 (auto-generate): totalAmount = fee  OR  subtotal + vat  (WHT ignored)
    Path 2 (legacy import): totalAmount = r.total  (WHT ignored)

  Root cause of 303 wrong records in company 4 (and any other company using
  billing-notes auto-generate).

SCREENS INVOLVED IN INVOICE CREATION (5 total):
  1. Invoice form — manual create (POST /api/invoices) — trusts frontend formula
  2. Invoice form — manual edit   (PATCH /api/invoices/:id) — trusts frontend formula
  3. Invoice clone                — copies source totalAmount
  4. Billing notes → auto-generate ✅ FIXED (this entry)
  5. Billing notes → legacy import ✅ FIXED (this entry)
  (ecommerce-import has no WHT by design — correct for that context)

FILES CHANGED:
  server/routes/billing-notes-routes.ts
    - Line 827-840: auto-generate path — now calculates wht first, then
      totalAmount = grossBeforeWht - wht
    - Line 1309-1316: legacy import path — now totalAmount = grossTotal - wht

RISK: Very low.
  - No DB schema change.
  - Only affects NEW records created going forward.
  - Existing wrong records are NOT touched by this change.
  - Cannot break any existing data.

VERIFY AFTER DEPLOY:
  Create a new invoice via billing-notes auto-generate for a client with WHT.
  Confirm: totalAmount in DB = subtotal + VAT - WHT (not subtotal + VAT).

NOTE:
  Screens 1, 2, 3 (manual form, edit, clone) trust the frontend formula.
  Frontend invoice-form.tsx already computes: grandTotal = subtotal + vat - wht ✅
  These 3 screens require พี่ทราย to manually test and confirm no wrong data.

========================================================
ENTRY #004 — sales_credit_notes: 4 etax columns (e-Tax Invoice credit note feature)
Date: 2026-05-07
Status: PENDING (dev DB has columns via auto-migration, production does NOT)
Approved by: (waiting)
========================================================

WHAT:
  4 new columns added to sales_credit_notes table.
  Added via runSalesCreditNoteEtaxMigration() in server/schema-extra.ts.

WHY:
  Feature: ส่ง e-Tax Invoice (ใบลดหนี้) ไปยังกรมสรรพากร
  Without these columns the send-email route fails when trying to record
  that the document was sent.
  - etax_sent_at: timestamp when e-Tax was sent
  - etax_sent_to: buyer email the PDF/A-3 was sent to
  - etax_sent_cc: ETDA timestamp email (CC)
  - etax_message_id: SMTP/Resend message ID for tracing

SQL TO RUN ON PRODUCTION (in order):

  ALTER TABLE sales_credit_notes ADD COLUMN IF NOT EXISTS etax_sent_at TIMESTAMP;
  ALTER TABLE sales_credit_notes ADD COLUMN IF NOT EXISTS etax_sent_to TEXT;
  ALTER TABLE sales_credit_notes ADD COLUMN IF NOT EXISTS etax_sent_cc TEXT;
  ALTER TABLE sales_credit_notes ADD COLUMN IF NOT EXISTS etax_message_id TEXT;

RISK: Very low.
  - IF NOT EXISTS: safe to re-run.
  - All nullable: no existing rows affected.
  - Additive only: no existing columns modified or dropped.

VERIFY AFTER RUNNING:
  SELECT column_name FROM information_schema.columns
  WHERE table_name = 'sales_credit_notes'
    AND column_name IN ('etax_sent_at','etax_sent_to','etax_sent_cc','etax_message_id')
  ORDER BY column_name;
  -- Expected: 4 rows

RELATED CODE FILES:
  server/schema-extra.ts — runSalesCreditNoteEtaxMigration()
  server/routes/etax-routes.ts — calls migration on startup; send-email writes these columns
  client/src/pages/sales/credit-note-form.tsx — EtaxCreditNoteButton reads etaxSentAt/etaxSentTo

========================================================
ENTRY #005 — (next change goes here)
========================================================
END OF FILE
========================================================
